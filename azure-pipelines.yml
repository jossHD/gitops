trigger:
- main

pool:
  name: PoC IBK

steps:

- task: GitHubRelease@1
  inputs:
    gitHubConnection: 'github-jossHD'
    repositoryName: '$(Build.Repository.Name)'
    action: 'create'
    target: '$(Build.SourceVersion)'
    tagSource: 'gitTag'
    title: 'sample'
    assets: '$(Build.ArtifactStagingDirectory)/README.txt'
    changeLogCompareToRelease: 'lastFullRelease'
    changeLogType: 'commitBased'

