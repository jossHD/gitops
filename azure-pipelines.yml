trigger:
- main

pool:
  name: PoC IBK

stages:
- stage: Prueba
  displayName: Prueba ACE
  jobs:
  - deployment: Pruebaace
    workspace:
      clean: all
    displayName: prueba en ace
    environment: sandbox
    strategy:
      runOnce:
        deploy:
         steps:
           - task: CmdLine@2
             inputs:
               script: 'mqsicreateworkdir --help'
